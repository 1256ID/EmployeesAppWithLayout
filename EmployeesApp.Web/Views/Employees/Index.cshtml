@model Employee[]

@{
    ViewBag.Title = "Index";
}

<ul>
    @foreach (Employee employee in Model)
    {
        <li>
            <a asp-controller="Employees" asp-action="Details" asp-route-id="@employee.Id">@employee.Name</a>
        </li>
    }
</ul>

<h3><a asp-controller="Employees" asp-action="Create">Create Employee</a></h3>
